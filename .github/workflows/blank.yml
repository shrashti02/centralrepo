name: slack
on: 
 workflow_dispatch:
  inputs:
    your_nmae:
     description: Name
     required: true  
jobs:
  slack_upload_job:
    runs-on: ubuntu-latest
    steps:
     - name: slack notigication
       if: success() || failure()
       uses: actions/notify-slack-action@v2
       with:
         status: "job status"
         notification_title: "slack-notification"
         slack-emoji-name: "fire"
         slack-channel: C04UMANJGG1
       env: 
         SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
         
     
      
      
        
